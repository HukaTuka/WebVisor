package dk.sea.webvisor.GUI;

// This is just a class so it splits up the Model folder instead of combining it with GUI folder. It is safe to delete
// when another class is added to the Model folder
public class Test {
}
